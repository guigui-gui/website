# guigui-website

The Guigui website, generated with
[`github.com/hajimehoshi/ssg`](https://github.com/hajimehoshi/ssg).

To build the site:

```sh
go run .
```

To serve the site locally with automatic regeneration:

```sh
go run . -http :8000
```
